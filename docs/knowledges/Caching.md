# DNS Caching

Goals:
- Reduce latency
- Reduce upstream traffic
- Respect TTL
- Maintain bounded memory

The cache is intentionally postponed until Sprint 04 to keep the forwarding path simple first.
