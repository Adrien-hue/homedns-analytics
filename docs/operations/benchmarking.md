# Benchmarking

Profiles:

-   Quick
-   Validation
-   Endurance

Run:

``` bash
make benchmark-quick
make benchmark-validation
make benchmark-endurance
```

Reports are stored under:

    /opt/homedns/shared/benchmarks/

Official baseline reports are stored in:

    benchmarks/baseline/

Never overwrite a baseline; create new comparison runs.
