# Running Benchmarks

Available profiles:

-   Quick
-   Validation
-   Endurance

Execute:

``` bash
make benchmark-quick
make benchmark-validation
make benchmark-endurance
```

Benchmark reports are written under:

    /opt/homedns/shared/benchmarks/

Compare new results against the baseline stored in:

    benchmarks/baseline/
