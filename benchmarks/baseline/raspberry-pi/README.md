# Raspberry Pi Baseline

These benchmarks are the official reference measurements for HomeDNS.

They must never be modified.

Every future release should be compared against these measurements to detect regressions or improvements.

Current baseline:

- v0.2.0
